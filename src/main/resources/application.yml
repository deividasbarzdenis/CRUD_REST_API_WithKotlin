spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: "jdbc:h2:mem:dev_d"
    username: "sa"
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
server:
  port: 8081
